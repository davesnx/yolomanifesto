language: node_js
node_js:
  - 8

cache:
  directories:
    - "node_modules"

before-script:
  - yarn run lint
  - yarn run build

script:
  - yarn test

after_success:
  - test $TRAVIS_BRANCH = "master" && test $TRAVIS_PULL_REQUEST = "false" &&
    now -t $TOKEN_NOW --public alias