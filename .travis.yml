language: node_js
node_js:
  - 8

cache:
  directories:
    - "node_modules"

before_script:
  - yarn run lint
  - yarn run build

script:
  - yarn test

after_script:
  - yarn add now
  - test $TRAVIS_BRANCH = "master" && now --token $TOKEN_NOW --public alias
